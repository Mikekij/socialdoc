<%= form_for(@tracked_site) do |f| %>
  <% if @tracked_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tracked_site.errors.count, "error") %> prohibited this tracked_site from being saved:</h2>

      <ul>
      <% @tracked_site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
